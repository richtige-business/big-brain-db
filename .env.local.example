NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: enables dense (pgvector) search in /api/brain/search.
# Without it, that endpoint falls back to lexical-only search.
OPENAI_API_KEY=
